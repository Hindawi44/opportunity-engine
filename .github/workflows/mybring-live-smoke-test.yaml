name: Mybring Live Smoke Test (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time Mybring smoke test completed successfully; no automatic trigger remains."
