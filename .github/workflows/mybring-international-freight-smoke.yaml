name: Mybring International Freight Quote Smoke Test (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time international freight smoke test completed; no automatic trigger remains."
