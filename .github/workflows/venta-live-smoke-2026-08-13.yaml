name: VENTA Live Smoke 2026-08-13 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary live smoke completed successfully and is disabled."
