name: Google Routes Live Smoke 2026-08-13 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled after successful one-time Google Routes live smoke test."
