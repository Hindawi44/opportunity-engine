name: MIND FORGE V1.10 Calibration Runs

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  calibration-complete:
    runs-on: ubuntu-latest
    steps:
      - name: Calibration complete
        run: echo "MIND FORGE V1.10 calibration is complete; paid calibration runs are disabled by default."
