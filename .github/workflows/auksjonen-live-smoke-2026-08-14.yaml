name: Auksjonen Exact Item Live Smoke 2026-08-14

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot live smoke completed successfully on run 31782624389; automatic execution disabled."
